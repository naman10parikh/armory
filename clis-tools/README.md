# clis-tools/ — 92 components (catalog view)

Each `<slug>.md` is a slim install card generated from `brain/components/clis-tools/`.

**Full metadata** → `brain/components/clis-tools/<slug>.md`

This directory is **generated** by `ingest/surface.mjs`. Do not hand-edit.
Run `node ingest/surface.mjs --apply` to rebuild.
