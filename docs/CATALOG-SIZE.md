# `catalog.json` is 52.6 MB — the ruling (CP138 T24)

**Decision: gzip it to `catalog.json.gz` and decompress at load. Not tonight.** Measured 2026-09-07.

## What is actually true

| | |
| --- | --- |
| `catalog.json` | **52.6 MB** — over GitHub's 50 MB soft recommendation (the hard limit is 100 MB) |
| gzipped | **6.9 MB — 87% smaller** |
| commits that have touched it | **136** |
| repo pack | **226 MB** |
| source of truth | **`brain/components/**/*.md` — 64,859 markdown files.** The JSON is derived |

The 50 MB warning is the symptom, not the disease. The disease is that a **derived** 52 MB artifact is
regenerated by the nightly autolab and committed, so the repository grows by a fresh revision of it
every night. 136 revisions is most of that 226 MB pack, and the curve is linear from here.

## Options considered

| Option | Effect | Cost |
| --- | --- | --- |
| **gzip → 6.9 MB** ✅ | kills the warning, cuts pack growth ~8×, artifact stays in git so the deploy is unchanged in shape | one read site in `lib/rank.mjs`, plus `scripts/copy-data.mjs` and the web build |
| Git LFS | removes blobs from pack history entirely | every clone and the Vercel build need LFS configured; a build that silently gets an LFS pointer instead of data fails in a confusing way |
| Stop committing it; build at deploy | conceptually correct — derived artifacts do not belong in git | building from 64,859 markdown files at deploy time, on every deploy |
| Split into 12 per-component files | each file under the limit | same total bytes, and every consumer changes |
| Accept and document | zero work | pack keeps growing ~52 MB of revisions per night |

## Why not tonight

Changing how the catalogue is read touches the hot runtime path shared by the site, the CLI and the
MCP server — and **production is currently returning 402 `DEPLOYMENT_DISABLED` account-wide (CP138
C-07)**, so a deploy cannot be verified as a user. Shipping a data-path change that cannot be tested
end-to-end is the exact thing the QA rule exists to stop. The measurement is the deliverable; the
change lands in the first session after prod is reachable, in this order:

1. `ingest/catalog.mjs` writes both `catalog.json.gz` and (transitionally) `catalog.json`
2. `lib/rank.mjs` prefers the `.gz`, falls back to the plain file
3. `scripts/copy-data.mjs` and the web build vendor the `.gz`
4. verify: `armory rank`, the MCP `rank_components`, and `/api/rank` on prod all return the same top
   row for the same query as they do today (`letta-ai-letta`, 99, memory)
5. only then stop writing the plain file, and `.gitignore` it

**Do not rewrite history to shrink the pack.** It breaks every existing clone and the nightly
autolab's checkout for a one-time gain that step 5 makes permanent anyway.

## 26 September 2026: steps 1 to 4 done, step 5 waits on seven readers

Measured today: `catalog.json` is 55,410,375 bytes (52.8 MiB), still over GitHub's 50 MiB warning.
Gzipped, it is 7,085,052 bytes (6.8 MiB).

1. `ingest/catalog.mjs` writes `catalog.json.gz` beside `catalog.json`.
2. `lib/rank.mjs` reads the `.gz` first, as long as it is at least as new as the plain file.
3. `web/scripts/copy-data.mjs` vendors only the `.gz` into `web/`, and the site reads it through
   `web/src/lib/catalog-file.ts`. `next.config.mjs` traces it, so the upload and every function carry
   7 MB, not 55 MB.
4. `armory rank`, the MCP's `rank_components` and `/api/rank` return the same top row: overall
   `openclaw-openclaw` (100), and for memory `thedotmack-claude-mem` (99.9). The ruling's
   `letta-ai-letta` moved when the blend changed (CP138 T20) and duplicates folded (T56).

**Step 5 is not done.** Seven readers outside Armory load the plain file by path:
- Sentinel: `access/brain_sync.py`, `access/emit_to_armory.py`, `access/enrich_stars.py`,
  `scripts/enrich-armory.py`, `web/lib/matrix.py`, `web/lib/universe.py`, and
  `scripts/armory-sync-pr.sh` (which commits it).
- Energy: `platform/lib/armory.mjs`.

So `catalog.json` is still written and committed. The `.gz` is gitignored, not committed, because
committing a gzip blob every night on top would make the pack grow faster, not slower. To finish,
point those seven at `catalog.json.gz` (or have them gunzip it), then stop committing `catalog.json`.

## 26 September 2026: a size guard until step 5 lands

While the plain file is committed, GitHub will refuse the push once it passes 100 MB (GitHub counts 1 MB as
1,048,576 bytes). `node scripts/catalog-size.mjs` prints the size, **warns at 80 MB** and **fails at 95 MB**.
It runs in CI after the freshness check, and in the nightly refresh (`autolab.yml`) before anything is
committed, so a failing night commits nothing. Sentinel's feed sync, which commits the file too, runs it
before its commit.

| Date | Size | Rows |
| --- | --- | --- |
| 28 May 2026 | 18.3 MB | |
| 3 Jul 2026 | 30.5 MB | 41,738 |
| 12 Aug 2026 | 43.2 MB | 58,814 |
| 5 Sep 2026 | 52.6 MB | 64,849 |
| 26 Sep 2026 | 52.9 MB | 65,238 |

About 850 bytes a row, so 80 MB is roughly 33,000 more rows: two months at the crawl-driven pace of July and
August, far longer at September's (389 rows in three weeks).

**At the warning:** finish step 5. If the seven readers are still not moved, write `catalog.json` without
indentation as a stopgap: the same content is 39.1 MB today, 26% smaller, and every reader parses JSON, so only
the diffs change (one long line).
