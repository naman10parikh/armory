# workflows/ — 716 components (catalog view)

Each `<slug>.md` is a slim install card generated from `brain/components/workflows/`.

**Full metadata** → `brain/components/workflows/<slug>.md`

This directory is **generated** by `ingest/surface.mjs`. Do not hand-edit.
Run `node ingest/surface.mjs --apply` to rebuild.
