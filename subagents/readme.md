# subagents/ — 730 components (catalog view)

Each `<slug>.md` is a slim install card generated from `brain/components/subagents/`.

**Full metadata** → `brain/components/subagents/<slug>.md`

This directory is **generated** by `ingest/surface.mjs`. Do not hand-edit.
Run `node ingest/surface.mjs --apply` to rebuild.
