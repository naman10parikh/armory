---
name: pdf-inspector
type: mcps
description: >
  Classifies PDFs (text-based vs scanned vs image vs mixed), extracts text, and converts to clean Markdown over Streamable HTTP MCP. Supports remote PDFs via URL or base64, with optional page restriction and OCR fallback detection.
source_repo: agenticledger/pdf-inspector-mcp-http
source_url: https://github.com/agenticledger/pdf-inspector-mcp-http
license: unknown
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: 0
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
Classifies PDFs (text-based vs scanned vs image vs mixed), extracts text, and converts to clean Markdown over Streamable HTTP MCP. Supports remote PDFs via URL or base64, with optional page restriction and OCR fallback detection.

## When to use it
Classifies PDFs (text-based vs scanned vs image vs mixed), extracts text, and converts to clean Markdown over Streamable HTTP MCP. Supports remote PDFs via URL or base64, with optional page restriction and OCR fallback detection.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/jcfatf170j) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
