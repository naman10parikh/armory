---
name: mcp-grep
type: mcps
description: >
  A grep server implementation that exposes grep functionality through the Model Context Protocol (MCP), supporting natural language prompts and common grep options such as case-insensitive matching, context lines, and recursive searching.
source_repo: fastmcp-me/mcp-grep-erniebrodeur
source_url: https://github.com/fastmcp-me/mcp-grep-erniebrodeur
license: GPL 3.0
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
A grep server implementation that exposes grep functionality through the Model Context Protocol (MCP), supporting natural language prompts and common grep options such as case-insensitive matching, context lines, and recursive searching.

## When to use it
A grep server implementation that exposes grep functionality through the Model Context Protocol (MCP), supporting natural language prompts and common grep options such as case-insensitive matching, context lines, and recursive searching.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/t2rn89z2yj) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
