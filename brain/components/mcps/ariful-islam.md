---
name: ariful-islam
type: mcps
description: >
  MCP servers for SQL databases. One package per engine (SQLite, libSQL, MySQL, MariaDB, Postgres), two tools, read-only by default, secrets never in logs.
source_repo: arifulislamat/database-mcp
source_url: https://github.com/arifulislamat/database-mcp
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
MCP servers for SQL databases. One package per engine (SQLite, libSQL, MySQL, MariaDB, Postgres), two tools, read-only by default, secrets never in logs.

## When to use it
MCP servers for SQL databases. One package per engine (SQLite, libSQL, MySQL, MariaDB, Postgres), two tools, read-only by default, secrets never in logs.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/tpbibvvz3l) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
