---
name: devcontainer-mcp
type: mcps
description: >
  A minimal MCP server that exposes a single bash tool to execute commands inside a devcontainer, returning real output and exit codes.
source_repo: vovayartsev-dice/devcontainer-mcp
source_url: https://github.com/vovayartsev-dice/devcontainer-mcp
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
A minimal MCP server that exposes a single bash tool to execute commands inside a devcontainer, returning real output and exit codes.

## When to use it
A minimal MCP server that exposes a single bash tool to execute commands inside a devcontainer, returning real output and exit codes.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/luh1bp7exl) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
