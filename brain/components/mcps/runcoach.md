---
name: runcoach
type: mcps
description: >
  An MCP server that exposes a runner's own training data — computed training load, race-time predictions, and generated plans — as tools an LLM can call for grounded coaching advice.
source_repo: varun-gangadharan/runcoach
source_url: https://github.com/varun-gangadharan/runcoach
license: unknown
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
An MCP server that exposes a runner's own training data — computed training load, race-time predictions, and generated plans — as tools an LLM can call for grounded coaching advice.

## When to use it
An MCP server that exposes a runner's own training data — computed training load, race-time predictions, and generated plans — as tools an LLM can call for grounded coaching advice.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/xpmie1r4kb) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
