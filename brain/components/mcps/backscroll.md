---
name: backscroll
type: mcps
description: >
  Records your terminal sessions per command (PTY + OSC 133) into local SQLite, so AI agents can search, retrieve, and diff what commands actually printed. Secret redaction is applied by default to everything served over MCP.
source_repo: soren-achebe/backscroll
source_url: https://github.com/soren-achebe/backscroll
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
Records your terminal sessions per command (PTY + OSC 133) into local SQLite, so AI agents can search, retrieve, and diff what commands actually printed. Secret redaction is applied by default to everything served over MCP.

## When to use it
Records your terminal sessions per command (PTY + OSC 133) into local SQLite, so AI agents can search, retrieve, and diff what commands actually printed. Secret redaction is applied by default to everything served over MCP.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/gtepsky8wy) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
