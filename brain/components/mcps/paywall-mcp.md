---
name: paywall-mcp
type: mcps
description: >
  Generic Lightning-paywall sidecar that wraps any stdio MCP server and requires a paid Lightning invoice (via NWC) before forwarding priced tool calls. Per-tool prices are set via env; includes payment verification, replay protection, and an audit log — the upstream server stays untouched.
source_repo: llmops-pro/paywall-mcp
source_url: https://github.com/llmops-pro/paywall-mcp
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
Generic Lightning-paywall sidecar that wraps any stdio MCP server and requires a paid Lightning invoice (via NWC) before forwarding priced tool calls. Per-tool prices are set via env; includes payment verification, replay protection, and an audit log — the upstream server stays untouched.

## When to use it
Generic Lightning-paywall sidecar that wraps any stdio MCP server and requires a paid Lightning invoice (via NWC) before forwarding priced tool calls. Per-tool prices are set via env; includes payment verification, replay protection, and an audit log — the upstream server stays untouched.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/yjorxous0a) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
