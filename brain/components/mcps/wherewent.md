---
name: wherewent
type: mcps
description: >
  Zero-config SQL profiler for slow Python/SQLAlchemy jobs, built for agents: returns the exact call site, query count, and fix as compact machine-readable fields instead of raw query logs~55× fewer tokens per diagnosis (≈10k → ≈180) and the matching drop in API cost.
source_repo: habibafaisal/wherewent
source_url: https://github.com/habibafaisal/wherewent
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
Zero-config SQL profiler for slow Python/SQLAlchemy jobs, built for agents: returns the exact call site, query count, and fix as compact machine-readable fields instead of raw query logs~55× fewer tokens per diagnosis (≈10k → ≈180) and the matching drop in API cost.

## When to use it
Zero-config SQL profiler for slow Python/SQLAlchemy jobs, built for agents: returns the exact call site, query count, and fix as compact machine-readable fields instead of raw query logs~55× fewer tokens per diagnosis (≈10k → ≈180) and the matching drop in API cost.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/va9m9h37ib) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
