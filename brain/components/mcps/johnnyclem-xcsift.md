---
name: johnnyclem-xcsift
type: mcps
description: >
  Wraps the xcsift CLI tool to parse Xcode build output into structured, token-efficient JSON or TOON formats, automatically managing the binary and providing tools to execute build commands with automatic parsing and extract specific errors, warnings, and test failures with file/line details.
source_repo: johnnyclem/xcsift-mcp
source_url: https://github.com/johnnyclem/xcsift-mcp
license: unknown
cli_compat: [claude, codex, cursor, gemini, opencode]
maturity: beta
stars: 7
verified_at: 2026-05-26
related: []
tags: [mcp, pulsemcp]
---
## What it is
MCP server `xcsift`, catalogued on PulseMCP. Wraps the xcsift CLI tool to parse Xcode build output into structured, token-efficient JSON or TOON formats, automatically managing the binary and providing tools to execute build commands with automatic parsing and extract specific errors, warnings, and test failures with file/line details.

## When to use it
Wraps the xcsift CLI tool to parse Xcode build output into structured, token-efficient JSON or TOON formats, automatically managing the binary and providing tools to execute build commands with automatic parsing and extract specific errors, warnings, and test failures with file/line details.

## How to install / invoke
See the source for the `mcpServers` config block (command + args). Source: https://github.com/johnnyclem/xcsift-mcp

## Notes
Discovered via the PulseMCP registry (https://www.pulsemcp.com/servers/johnnyclem-xcsift). License not declared in registry metadata — confirm before production use. Pending verify -> promote.
