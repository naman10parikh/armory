---
name: mcp-tool-ai
type: mcps
description: >
  A configurable MCP gateway that runs multiple Streamable HTTP MCP servers and exposes all their tools through a single endpoint, enabling tool aggregation and routing for MCP clients.
source_repo: dkokane0/mcp-tool-ai
source_url: https://github.com/dkokane0/mcp-tool-ai
license: unknown
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
A configurable MCP gateway that runs multiple Streamable HTTP MCP servers and exposes all their tools through a single endpoint, enabling tool aggregation and routing for MCP clients.

## When to use it
A configurable MCP gateway that runs multiple Streamable HTTP MCP servers and exposes all their tools through a single endpoint, enabling tool aggregation and routing for MCP clients.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/zv8pwjsspz) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
