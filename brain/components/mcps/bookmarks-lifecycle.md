---
name: bookmarks-lifecycle
type: mcps
description: >
  A local MCP server that gives saved bookmarks a lifecycle with time-based layers (pending, watching, kept, lapsed), so they get decided on instead of accumulating. It aggregates bookmarks from your browsers, drips them into a daily queue, and supports keep/watch/drop decisions, restore, and undo.
source_repo: Fisher521/bookmarks-lifecycle
source_url: https://github.com/Fisher521/bookmarks-lifecycle
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: 0
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
A local MCP server that gives saved bookmarks a lifecycle with time-based layers (pending, watching, kept, lapsed), so they get decided on instead of accumulating. It aggregates bookmarks from your browsers, drips them into a daily queue, and supports keep/watch/drop decisions, restore, and undo.

## When to use it
A local MCP server that gives saved bookmarks a lifecycle with time-based layers (pending, watching, kept, lapsed), so they get decided on instead of accumulating. It aggregates bookmarks from your browsers, drips them into a daily queue, and supports keep/watch/drop decisions, restore, and undo.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/o6j2lareu8) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
