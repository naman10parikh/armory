---
name: claude-openai-image-mcp
type: mcps
description: >
  A security-first MCP server that generates images via OpenAI's image model (gpt-image-2) and returns them as MCP image content, usable from Claude Code, Claude Desktop, and other stdio MCP clients.
source_repo: theosera/claude_openai_image_mcp
source_url: https://github.com/theosera/claude_openai_image_mcp
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
A security-first MCP server that generates images via OpenAI's image model (gpt-image-2) and returns them as MCP image content, usable from Claude Code, Claude Desktop, and other stdio MCP clients.

## When to use it
A security-first MCP server that generates images via OpenAI's image model (gpt-image-2) and returns them as MCP image content, usable from Claude Code, Claude Desktop, and other stdio MCP clients.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/ymtqs3pej3) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
