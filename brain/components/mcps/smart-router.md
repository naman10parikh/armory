---
name: smart-router
type: mcps
description: >
  A single MCP server that fronts many downstream MCP servers and Skills, exposing only four tools (search, call_tool, use_skill, admin) so that an agent's context window only ever sees search results on demand.
source_repo: chris-asmussen/warden
source_url: https://github.com/chris-asmussen/warden
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
A single MCP server that fronts many downstream MCP servers and Skills, exposing only four tools (search, call_tool, use_skill, admin) so that an agent's context window only ever sees search results on demand.

## When to use it
A single MCP server that fronts many downstream MCP servers and Skills, exposing only four tools (search, call_tool, use_skill, admin) so that an agent's context window only ever sees search results on demand.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/qnmjf443e2) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
