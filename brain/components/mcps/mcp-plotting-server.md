---
name: mcp-plotting-server
type: mcps
description: >
  A FastMCP server that turns JSON data into Plotly figures, deployable as an isolated service on Modal. Data goes in as JSON; a validated Plotly figure (JSON) or a rendered PNG comes back over the Model Context Protocol.
source_repo: ericmjl/mcp-plotting-server
source_url: https://github.com/ericmjl/mcp-plotting-server
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: 1
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
A FastMCP server that turns JSON data into Plotly figures, deployable as an isolated service on Modal. Data goes in as JSON; a validated Plotly figure (JSON) or a rendered PNG comes back over the Model Context Protocol.

## When to use it
A FastMCP server that turns JSON data into Plotly figures, deployable as an isolated service on Modal. Data goes in as JSON; a validated Plotly figure (JSON) or a rendered PNG comes back over the Model Context Protocol.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/lbklg45gyh) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
