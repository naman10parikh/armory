---
name: smallminds-mongtap
type: mcps
description: >
  Provides a MongoDB-compatible server backed by DataFlood statistical modeling, enabling database creation and querying using natural language without requiring actual data storage while generating synthetic documents with deterministic or probabilistic patterns.
source_repo: smallmindsco/mongtap
source_url: https://github.com/smallmindsco/mongtap
license: unknown
cli_compat: [claude, codex, cursor, gemini, opencode]
maturity: beta
stars: 4
verified_at: 2026-05-26
related: []
tags: [mcp, pulsemcp]
---
## What it is
MCP server `MongTap`, catalogued on PulseMCP. Provides a MongoDB-compatible server backed by DataFlood statistical modeling, enabling database creation and querying using natural language without requiring actual data storage while generating synthetic documents with deterministic or probabilistic patterns.

## When to use it
Provides a MongoDB-compatible server backed by DataFlood statistical modeling, enabling database creation and querying using natural language without requiring actual data storage while generating synthetic documents with deterministic or probabilistic patterns.

## How to install / invoke
See the source for the `mcpServers` config block (command + args). Source: https://github.com/smallmindsco/mongtap

## Notes
Discovered via the PulseMCP registry (https://www.pulsemcp.com/servers/smallminds-mongtap). License not declared in registry metadata — confirm before production use. Pending verify -> promote.
