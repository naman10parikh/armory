---
name: pipeworx-docker-hub
type: mcps
description: >
  Docker Hub public registry data via the Docker Hub v2 API, no authentication required.
source_repo: pipeworx-io/mcp-dockerhub
source_url: https://github.com/pipeworx-io/mcp-dockerhub
license: unknown
cli_compat: [claude, codex, cursor, gemini, opencode]
maturity: beta
stars: null
verified_at: 2026-05-26
related: []
tags: [mcp, pulsemcp]
---
## What it is
MCP server `Docker Hub`, catalogued on PulseMCP. Docker Hub public registry data via the Docker Hub v2 API, no authentication required.

## When to use it
Docker Hub public registry data via the Docker Hub v2 API, no authentication required.

## How to install / invoke
See the source for the `mcpServers` config block (command + args). Source: https://github.com/pipeworx-io/mcp-dockerhub

## Notes
Discovered via the PulseMCP registry (https://www.pulsemcp.com/servers/pipeworx-docker-hub). License not declared in registry metadata — confirm before production use. Pending verify -> promote.
