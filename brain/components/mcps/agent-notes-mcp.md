---
name: agent-notes-mcp
type: mcps
description: >
  Lets MCP clients search, read, create, append, replace, move, and batch-update local Markdown notes safely, with conflict detection via SHA-256 and optional managed update blocks, without requiring a vector database.
source_repo: mitszo/agent-notes-mcp
source_url: https://github.com/mitszo/agent-notes-mcp
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: 0
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
Lets MCP clients search, read, create, append, replace, move, and batch-update local Markdown notes safely, with conflict detection via SHA-256 and optional managed update blocks, without requiring a vector database.

## When to use it
Lets MCP clients search, read, create, append, replace, move, and batch-update local Markdown notes safely, with conflict detection via SHA-256 and optional managed update blocks, without requiring a vector database.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/uqmloow5dp) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
