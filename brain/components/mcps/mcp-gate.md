---
name: mcp-gate
type: mcps
description: >
  Enables serving multiple MCP toolkits behind one server with capability-based access control, so different callers see and can call only the tools they are authorized for, over stdio or streamable HTTP with bearer-token auth.
source_repo: RunLit/mcp-gate
source_url: https://github.com/RunLit/mcp-gate
license: MIT License
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: null
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
Enables serving multiple MCP toolkits behind one server with capability-based access control, so different callers see and can call only the tools they are authorized for, over stdio or streamable HTTP with bearer-token auth.

## When to use it
Enables serving multiple MCP toolkits behind one server with capability-based access control, so different callers see and can call only the tools they are authorized for, over stdio or streamable HTTP with bearer-token auth.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/s7lmcojsaj) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
