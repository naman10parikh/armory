---
name: aimcpgate
type: mcps
description: >
  MCP gateway/proxy: multiplexes tool calls across upstream MCP servers into one aggregated, namespaced catalog and logs every call. Local, single-user, $0/month by default.
source_repo: akomyagin/aiMCPGate
source_url: https://github.com/akomyagin/aiMCPGate
license: unknown
cli_compat: [claude, cursor, codex, opencode, gemini]
maturity: experimental
stars: 0
eval_score: null
verified_at: 2026-05-27
related: []
tags: [glama, mcp]
---
## What it is
MCP gateway/proxy: multiplexes tool calls across upstream MCP servers into one aggregated, namespaced catalog and logs every call. Local, single-user, $0/month by default.

## When to use it
MCP gateway/proxy: multiplexes tool calls across upstream MCP servers into one aggregated, namespaced catalog and logs every call. Local, single-user, $0/month by default.

## How to install / invoke
See [Glama](https://glama.ai/mcp/servers/kifsclubfe) for the install config.

## Notes
Discovered via the Glama MCP registry (live API). Pending verify -> promote.
