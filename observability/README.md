# observability/ — 36 components (catalog view)

Each `<slug>.md` is a slim install card generated from `brain/components/observability/`.

**Full metadata** → `brain/components/observability/<slug>.md`

This directory is **generated** by `ingest/surface.mjs`. Do not hand-edit.
Run `node ingest/surface.mjs --apply` to rebuild.
